{
    "scripts": {
        "start": "live-server client, --html=localhost --port=5500 --no-browser",
        "server": "node server/index.js"
    },
    "devDependencies": {
        "eslint": "^8.44.0",
        "live-server": "^1.2.2",
        "prettier": "^2.8.8"
    },
    "dependencies": {
        "gsap": "^3.12.2"
    }
}
